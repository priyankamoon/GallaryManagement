"""
Url patterns
"""

from django.urls import path
from workflow import views

urlpatterns = [
    path('get-hierarchy', views.WorkflowHierarchy.as_view()),
    path('get-transactions', views.WorkflowTransactions.as_view()),
    path('getapprovalmodel', views.GetApprovalModel.as_view()),
    path('approval', views.UpdateWorkflow.as_view()),
    # path('<int:id>', views.Workflow.as_view()),
    # path('<str:model>', views.Workflow.as_view()),
]
